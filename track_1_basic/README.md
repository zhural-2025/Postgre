# Track 1 (Basic)

## Setup

1. Create and activate virtual environment.
2. Install dependencies:
   - `pip install -r requirements.txt`
3. Create `.env` from `.env.example` and set DB credentials.

## Run

- `python main.py`

Expected result: rows from `users` are printed to console.
